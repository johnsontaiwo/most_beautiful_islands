require 'spec_helper'

describe MostBeautifulIslandsCliGem do
  it 'has a version number' do
    expect(MostBeautifulIslandsCliGem::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
